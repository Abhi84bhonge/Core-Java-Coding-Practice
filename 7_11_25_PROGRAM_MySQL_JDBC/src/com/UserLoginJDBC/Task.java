package com.UserLoginJDBC;

public class Task {

	
//	Task: JDBC userLogin
//
//	Write a JDBC code which will create database, create table with attribute id primary key and auto increment, full name, username,
//	password, createdAt will take default datetime current timestamp, and aadhar file upload.
//
//	Then app will have showUsers() which will only show id, name, createdAt (will not show username and password).
//
//	App will also have signUp() which will insert data into table.
//
//	Then app will have loginUser() which will accept only username and password, and it will match the values
//	whether the username & matching password is present in table or not.
//
//	If matched, then login; otherwise show invalid user message.

}
